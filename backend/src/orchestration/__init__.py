"""Planner, task engine, run manager, approvals, events. Implemented in
Phase 2 — except handoffs.py (agent-to-agent access policy enforcement),
built in Phase 1 alongside the Agent Gateway since §6.4 names this module
as the enforcement point.
"""
